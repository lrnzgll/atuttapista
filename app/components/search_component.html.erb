<div class='form-container'>
  <%= form_with url: search_page_path, local: true, method: :get do |form| %>
    <div class='search-tip hidden ta-c'>Cerca una itinerario</div>
    <%= form.text_field :search, placeholder: "Trova il tuo prossimo itinerario",  class: 'banner-input' %>
      <%= form.submit 'Cerca', class:"btn btn--stripe banner-submit" %>
  <% end %>
</div>
